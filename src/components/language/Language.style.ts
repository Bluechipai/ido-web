import styled from 'styled-components'

export const LanguageStyle = styled.div<{isH5: boolean}>`
  //width: ${({isH5}) => (isH5 ? '0.9rem' : '1.24rem')};
  height: 0.36rem;
  border-radius: 0.3rem;
`

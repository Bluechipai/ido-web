import styled from 'styled-components'

export const SwapPanelStyle = styled.a`
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0.4rem;
    right: 0;
    width: 100%;
    height: 0.4rem;
    border-radius: 0.12rem;
    background: rgba(0, 0, 0, 0.80);
    backdrop-filter: blur(0.2rem);
    font-size: 0.14rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.6);
    cursor: pointer;
`;
